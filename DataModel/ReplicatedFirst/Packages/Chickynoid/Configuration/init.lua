local module = {}

game.ReplicatedStorage:WaitForChild("Characters")

module.modules = {}
function module:LoadConfiguration(configName)
    
    for key,value in pairs(game.ReplicatedStorage.Characters:GetDescendants()) do
                
        if (module.W)
        
    end
end

return module
